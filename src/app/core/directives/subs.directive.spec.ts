import { SubsDirective } from './subs.directive';

describe('SubsDirective', () => {
  it('should create an instance', () => {
    const directive = new SubsDirective();
    expect(directive).toBeTruthy();
  });
});
